# Gaia Lang Agent Context

Use `gaia-lsp-tool check <path> --fail-on-blocking` before compiling a Gaia
package. The tool reports syntax, invalid DSL call shapes, legacy prior
dictionaries, unresolved strict references, and export-list issues in a JSON
envelope suitable for agent workflows.
