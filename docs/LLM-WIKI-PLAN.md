# gaia-lsp LLM Wiki Plan

This initial repo keeps the wiki scope small:

- Capture Gaia v0.5 DSL authoring symbols.
- Capture static diagnostics that reduce agent iteration loops before `gaia build compile`.
- Keep source provenance tied to `SiliconEinstein/Gaia` docs and examples.
- Expand later with runtime compiler diagnostics only behind an explicit opt-in command.
